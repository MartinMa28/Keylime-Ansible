---
- name: install keylime
  hosts: all
  remote_user: root

  tasks:
    - name: install keylime and its dependencies
      dnf:
        name: '{{ item }}'
        state: present
      loop:
        - keylime
        - wget
        - gcc
        - make
        - openssl-devel
        - vim
        - iputils
